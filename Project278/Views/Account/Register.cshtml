@model RegisterViewModel
@{
    ViewBag.Title = "Register";
    var errorMessage = TempData["Error"] as string;
}

<ul class="nav nav-pills nav-justified mb-3" id="ex1" role="tablist">
  <li class="nav-item" role="presentation">
    <a class="nav-link" id="tab-login" data-mdb-toggle="pill" href="/Account/Login" role="tab"
      aria-controls="pills-login" aria-selected="true">Login</a>
  </li>
  <li class="nav-item active" role="presentation">
    <a class="nav-link active" id="tab-register" data-mdb-toggle="pill" href="/Account/Register" role="tab"
      aria-controls="pills-register" aria-selected="false">Register</a>
  </li>
</ul>
<!-- Pills navs -->

<!-- Pills content -->
<div class="tab-content">
  <div class="tab-pane fade show active" id="pills-login" role="tabpanel" aria-labelledby="tab-login">
      @if (!string.IsNullOrEmpty(errorMessage))
      {
        <h4 class="text-danger p-2  ">@errorMessage</h4>
      }
      <form asp-controler="Account" asp-action="Register" asp-controller="Account" method="post">
      <!-- Email input -->
      <div class="form-outline mb-4">
        <span asp-validation-for="Username" class="text-danger text-requirement"></span>
        <input type="text" name="username" id="loginName" asp-for="Username" class="form-control" required/>
        <label class="form-label" for="loginName">Username</label>
      </div>

      <div class="form-outline mb-4">
        <span asp-validation-for="Email" class="text-danger text-requirement"></span>
        <input type="email" name="email" id="email" asp-for="Email" class="form-control" required/>
        <label class="form-label" for="email">Email</label>
      </div>

      <!-- Password input -->
      <div class="form-outline mb-4">
        <span asp-validation-for="Password" class="text-danger text-requirement"></span>
        <input type="password" name="password" asp-for="Password" id="password" class="form-control" required/>
        <label class="form-label" for="password">Password</label>
      </div>

      <!-- 2 column grid layout -->
      <div class="row mb-4">
        <div class="col-md-6 d-flex justify-content-start">
          <!-- Checkbox -->
          <div class="form-check mb-3 mb-md-0">
            <input class="form-check-input" type="checkbox" value="" id="loginCheck" checked />
            <label class="form-check-label" for="loginCheck"> Remember me </label>
          </div>
        </div>
      </div>

      <!-- Submit button -->
      <button type="submit" class="btn btn-primary btn-block mb-4">Register</button>

      <!-- Register buttons -->
      <div class="text-center">
        <p>Already a member? <a asp-action="Login" asp-controller="Account">Log In</a></p>
      </div>
    </form>
  </div>
</div>
<!-- Pills content -->
@section Scripts {
    @{
        <partial name="_validationScriptsPartial" />
    }
}
